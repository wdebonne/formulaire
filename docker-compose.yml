version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3110:3000"
    volumes:
      - sqlite-data:/app/prisma
      - uploads-data:/app/public/uploads
    restart: unless-stopped

volumes:
  sqlite-data:
  uploads-data:
