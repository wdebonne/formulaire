version: '3.8'
services:
  app:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run build && npm run start"
    ports:
      - "3110:3000"
    restart: unless-stopped
